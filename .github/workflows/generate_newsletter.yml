name: Génération et Déploiement de Newsletter

on:
  schedule:
    - cron: '0 9 * * 5'  # Tous les vendredis à 9h00
  workflow_dispatch:     # Permet le déclenchement manuel

# Permissions nécessaires pour écrire dans le repo et déployer GitHub Pages
permissions:
  contents: write
  pages: write
  id-token: write

# Configuration de l'environnement GitHub Pages
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  generate-newsletter:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout du dépôt newsletter-portfolio
      uses: actions/checkout@v3
      with:
        path: newsletter-portfolio
    
    - name: Checkout du dépôt PORTFOLIO
      uses: actions/checkout@v3
      with:
        repository: SiaSia-dev/PORTFOLIO 
        path: PORTFOLIO
        token: ${{ secrets.DEPLOY_TOKEN }}
        ref: main 
    
    - name: Checkout du dépôt portfolio-automation
      uses: actions/checkout@v3
      with:
        repository: SiaSia-dev/portfolio-automation
        path: portfolio-automation
        token: ${{ secrets.DEPLOY_TOKEN }}
    
    - name: Configuration de Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Installation des dépendances
      run: |
        python -m pip install --upgrade pip
        pip install PyYAML markdown beautifulsoup4 requests
    
    - name: Préparer le dossier de build
      run: |
        mkdir -p build/newsletters
        mkdir -p build/img
    
    - name: Exécution du générateur de newsletter
      run: |
        python portfolio-automation/scripts/newsletter_generator.py
      env:
        PORTFOLIO_DIR: ${{ github.workspace }}/PORTFOLIO
        OUTPUT_DIR: ${{ github.workspace }}/build/newsletters
        CREATE_INDEX: true
        CREATE_LATEST: true
        CREATE_ARCHIVES: true
    
    - name: Organiser les fichiers pour GitHub Pages
      run: |
        # Copier les fichiers HTML
        cp -v build/newsletters/*.html build/
        
        # Créer un index.html à la racine si nécessaire
        if [ ! -f "build/index.html" ]; then
          echo '<html><head><meta http-equiv="refresh" content="0;url=latest.html"></head><body><p>Redirection...</p></body></html>' > build/index.html
        fi
        
        # Copier les images directement depuis PORTFOLIO
        cp -rv PORTFOLIO/img/* build/img/ || echo "Pas d'images à copier"
        
        # Créer un .nojekyll pour GitHub Pages
        touch build/.nojekyll
    
    - name: Publier la newsletter avec github_pages_publisher.py
      run: |
        python portfolio-automation/scripts/github_pages_publisher.py
      env:
        NEWSLETTERS_DIR: ${{ github.workspace }}/build/newsletters
        GITHUB_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
    
    - name: Publication sur LinkedIn si configuré
      if: ${{ env.LINKEDIN_ACCESS_TOKEN != '' && env.LINKEDIN_PERSON_ID != '' }}
      run: |
        python portfolio-automation/scripts/linkedin_publisher.py
      env:
        LINKEDIN_ACCESS_TOKEN: ${{ secrets.LINKEDIN_ACCESS_TOKEN }}
        LINKEDIN_PERSON_ID: ${{ secrets.LINKEDIN_PERSON_ID }}
        NEWSLETTERS_DIR: ${{ github.workspace }}/build/newsletters
        GITHUB_USERNAME: SiaSia-dev
        GITHUB_REPO: newsletter-portfolio
    
    - name: Notification par email
      uses: dawidd6/action-send-mail@v3
      with:
        server_address: ${{ secrets.MAIL_SERVER }}
        server_port: ${{ secrets.MAIL_PORT }}
        username: ${{ secrets.MAIL_USERNAME }}
        password: ${{ secrets.MAIL_PASSWORD }}
        subject: 🔔 Newsletter Portfolio générée - À vérifier
        body: |
          Bonjour,
          
          La newsletter de cette semaine a été générée et est prête pour votre validation.
          
          URL de prévisualisation: https://siasia-dev.github.io/newsletter-portfolio/latest.html
          
          Si le contenu vous convient, vous pouvez déclencher manuellement la publication LinkedIn 
          via l'interface GitHub Actions.
          
          Bonne journée !
        to: ${{ secrets.NOTIFICATION_EMAIL }}
        from: Newsletter Portfolio <${{ secrets.MAIL_USERNAME }}>
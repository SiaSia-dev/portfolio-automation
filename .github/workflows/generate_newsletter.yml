name: Génération et Déploiement de Newsletter

on:
  schedule:
    - cron: '0 9 * * 5'  # Tous les vendredis à 9h00
  workflow_dispatch:     # Permet le déclenchement manuel

# Permissions nécessaires pour écrire dans le repo et déployer GitHub Pages
permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  generate-and-deploy-newsletter:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout du dépôt newsletter-portfolio
      uses: actions/checkout@v3
      with:
        repository: SiaSia-dev/newsletter-portfolio
        path: newsletter-portfolio
        token: ${{ secrets.DEPLOY_TOKEN }}
        ref: gh-pages
        fetch-depth: 0
    
    - name: Checkout du dépôt PORTFOLIO
      uses: actions/checkout@v3
      with:
        repository: SiaSia-dev/PORTFOLIO 
        path: PORTFOLIO
        token: ${{ secrets.DEPLOY_TOKEN }}
        ref: main 
    
    - name: Checkout du dépôt portfolio-automation
      uses: actions/checkout@v3
      with:
        repository: SiaSia-dev/portfolio-automation
        path: portfolio-automation
        token: ${{ secrets.DEPLOY_TOKEN }}
    
    - name: Configuration de Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Installation des dépendances
      run: |
        python -m pip install --upgrade pip
        pip install PyYAML markdown beautifulsoup4 requests
    
    - name: Préparer le dossier de build
      run: |
        mkdir -p build/newsletters
        mkdir -p build/img
    
    - name: Exécution du générateur de newsletter
      run: |
        cd portfolio-automation
        python scripts/newsletter_generator.py
      env:
        PORTFOLIO_DIR: ${{ github.workspace }}/PORTFOLIO
        OUTPUT_DIR: ${{ github.workspace }}/build/newsletters
        CREATE_INDEX: true
        CREATE_LATEST: true
        CREATE_ARCHIVES: true
    
    - name: Préparer les fichiers pour le déploiement
      run: |
        cp -R build/newsletters/* newsletter-portfolio/
        touch newsletter-portfolio/.nojekyll
    
    - name: Git configuration
      working-directory: newsletter-portfolio
      run: |
        git config user.name "GitHub Actions Bot"
        git config user.email "<>"
        git add .
        git commit -m "Deploy newsletter" || echo "No changes to commit"
    
    - name: Déploiement sur GitHub Pages
      uses: JamesIves/github-pages-deploy-action@v4
      with:
        token: ${{ secrets.DEPLOY_TOKEN }}
        repository: SiaSia-dev/newsletter-portfolio
        branch: gh-pages
        folder: newsletter-portfolio
        clean: true
    
    - name: Notification par email
      uses: dawidd6/action-send-mail@v3
      with:
        server_address: ${{ secrets.MAIL_SERVER }}
        server_port: ${{ secrets.MAIL_PORT }}
        username: ${{ secrets.MAIL_USERNAME }}
        password: ${{ secrets.MAIL_PASSWORD }}
        subject: 🔔 Newsletter Portfolio générée - À vérifier
        body: |
          Bonjour,
          
          La newsletter de cette semaine a été générée et est prête pour votre validation.
          
          URL de prévisualisation: https://siasia-dev.github.io/newsletter-portfolio/latest.html
          
          Bonne journée !
        to: ${{ secrets.NOTIFICATION_EMAIL }}
        from: Newsletter Portfolio <${{ secrets.MAIL_USERNAME }}>
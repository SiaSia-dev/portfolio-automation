name: Génération et Déploiement de Newsletter
on:
  schedule:
    - cron: '0 9 * * 5'  # Tous les vendredis à 9h00
  workflow_dispatch:     # Permet le déclenchement manuel

# Permissions nécessaires pour écrire dans le repo et déployer GitHub Pages
permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  generate-newsletter:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout du dépôt portfolio-automation
      uses: actions/checkout@v3
        
    - name: Checkout du dépôt PORTFOLIO
      uses: actions/checkout@v3
      with:
        repository: SiaSia-dev/PORTFOLIO 
        path: PORTFOLIO
        token: ${{ secrets.DEPLOY_TOKEN }}
        ref: main 
    
    - name: Vérifier le contenu du dépôt PORTFOLIO
      run: |
        echo "Contenu du répertoire PORTFOLIO :"
        ls -la PORTFOLIO/
        
        echo "Contenu du répertoire PORTFOLIO/docs :"
        ls -la PORTFOLIO/docs/ || echo "Le dossier docs n'existe pas"
        
        echo "Recherche de fichiers markdown dans PORTFOLIO :"
        find PORTFOLIO -name "*.md" | head -10
    
    - name: Checkout du dépôt newsletter-portfolio
      uses: actions/checkout@v3
      with:
        repository: SiaSia-dev/newsletter-portfolio
        path: newsletter-portfolio
        token: ${{ secrets.DEPLOY_TOKEN }}
        ref: gh-pages
    
    - name: Configuration de Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Installation des dépendances
      run: |
        python -m pip install --upgrade pip
        pip install PyYAML markdown beautifulsoup4 requests
    
    # Utilisons le chemin qui fonctionnait précédemment
    - name: Exécuter le générateur de newsletter
      run: |
        python scripts/newsletter_generator.py
        echo "Vérification des fichiers générés :"
        ls -la newsletters/
      env:
        # Utiliser le chemin exact qui fonctionnait avant
        PORTFOLIO_DIR: ${{ github.workspace }}/PORTFOLIO
        CONTENT_COUNT: 6
        REQUIRE_YAML: false
        OUTPUT_DIR: ${{ github.workspace }}/newsletters
    
    - name: Préparer le dossier de destination
      run: mkdir -p newsletter-portfolio/newsletters
    
    - name: Copier les fichiers HTML
      run: |
        echo "Copie des fichiers HTML vers newsletter-portfolio..."
        cp -r newsletters/*.html newsletter-portfolio/newsletters/ || echo "Aucun fichier HTML trouvé"
    
    - name: Copier les images
      run: |
        if [ -d "newsletters/img" ]; then
          echo "Copie des images..."
          mkdir -p newsletter-portfolio/newsletters/img
          cp -r newsletters/img/* newsletter-portfolio/newsletters/img/ || echo "Aucune image trouvée"
        else
          echo "Dossier d'images introuvable"
        fi
    
    - name: Créer index.html et .nojekyll
      run: |
        echo "Création du fichier index.html..."
        echo "<html><head><meta http-equiv='refresh' content='0;url=./newsletters/latest.html'></head><body>Redirection...</body></html>" > newsletter-portfolio/index.html
        
        echo "Création du fichier .nojekyll..."
        touch newsletter-portfolio/.nojekyll
    
    - name: Vérifier les fichiers avant déploiement
      run: |
        echo "Contenu du dossier newsletters :"
        ls -la newsletters/
        
        echo "Contenu du dossier newsletter-portfolio/newsletters :"
        ls -la newsletter-portfolio/newsletters/
    
    - name: Déploiement sur GitHub Pages
      uses: JamesIves/github-pages-deploy-action@v4
      with:
        repository: SiaSia-dev/newsletter-portfolio
        branch: gh-pages
        folder: newsletter-portfolio
        token: ${{ secrets.DEPLOY_TOKEN }}
        clean: true
        
    - name: Envoyer une notification par email
      uses: dawidd6/action-send-mail@v3
      with:
        server_address: ${{ secrets.MAIL_SERVER }}
        server_port: ${{ secrets.MAIL_PORT }}
        username: ${{ secrets.MAIL_USERNAME }}
        password: ${{ secrets.MAIL_PASSWORD }}
        subject: 🔔 Newsletter Portfolio générée - À vérifier
        body: |
          Bonjour,
          
          La newsletter de cette semaine a été générée et est prête pour votre validation.
          
          URL de prévisualisation: https://siasia-dev.github.io/newsletter-portfolio/newsletters/latest.html
          
          Si le contenu vous convient, vous pouvez déclencher manuellement la publication LinkedIn 
          via l'interface GitHub Actions.
          
          Bonne journée !
        to: ${{ secrets.NOTIFICATION_EMAIL }}
        from: Newsletter Portfolio <${{ secrets.MAIL_USERNAME }}>
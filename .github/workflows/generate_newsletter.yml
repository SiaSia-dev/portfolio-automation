name: Génération et Déploiement de Newsletter
on:
  schedule:
    - cron: '0 9 * * 5'  # Tous les vendredis à 9h00
  workflow_dispatch:     # Permet le déclenchement manuel

# Permissions nécessaires pour écrire dans le repo et déployer GitHub Pages
permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  generate-newsletter:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout du dépôt portfolio-automation
      uses: actions/checkout@v3
        
    - name: Checkout du dépôt PORTFOLIO
      uses: actions/checkout@v3
      with:
        repository: SiaSia-dev/PORTFOLIO 
        path: PORTFOLIO
        token: ${{ secrets.DEPLOY_TOKEN }}
        ref: main 
    
    - name: Vérifier la structure du dépôt PORTFOLIO
      run: |
        echo "Contenu du dépôt PORTFOLIO:"
        ls -la PORTFOLIO/
        
        if [ -d "PORTFOLIO/docs" ]; then
          echo "Dossier docs trouvé. Contenu:"
          ls -la PORTFOLIO/docs/
        else
          echo "ATTENTION: Le dossier docs n'existe pas dans le dépôt PORTFOLIO!"
        fi
        
        echo "Recherche de fichiers markdown:"
        find PORTFOLIO -name "*.md" | sort
    
    - name: Checkout du dépôt newsletter-portfolio
      uses: actions/checkout@v3
      with:
        repository: SiaSia-dev/newsletter-portfolio
        path: newsletter-portfolio
        token: ${{ secrets.DEPLOY_TOKEN }}
        ref: gh-pages
    
    - name: Configuration de Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Installation des dépendances
      run: |
        python -m pip install --upgrade pip
        pip install PyYAML markdown beautifulsoup4 requests
    
    # Modification importante : utilisation de PORTFOLIO directement comme docs_directory
    - name: Exécuter le générateur de newsletter
      run: |
        # Modification temporaire du script pour afficher plus d'informations
        sed -i 's/logger\.info(f"Recherche de fichiers Markdown récents dans {docs_directory}")/logger.info(f"DBUG: portfolio_directory={portfolio_directory}, docs_directory={docs_directory}")/' scripts/newsletter_generator.py
        
        # Dans ce cas, docs_directory sera déjà le chemin vers le dossier contenant les .md
        # Nous contournons donc la logique qui ajoute '/docs' au chemin
        python scripts/newsletter_generator.py
      env:
        # IMPORTANT: on définit PORTFOLIO_DIR pour qu'il pointe vers le dossier contenant les fichiers .md
        # Si les fichiers sont dans PORTFOLIO/docs, utilisez cette ligne:
        PORTFOLIO_DIR: ${{ github.workspace }}/PORTFOLIO
        # Si les fichiers sont à la racine de PORTFOLIO, décommentez cette ligne à la place:
        # PORTFOLIO_DIR: ${{ github.workspace }}/PORTFOLIO
        # Pour définir directement le dossier docs, essayez cette approche:
        DOCS_DIRECTORY: ${{ github.workspace }}/PORTFOLIO/docs
        CONTENT_COUNT: 6
        REQUIRE_YAML: false
        OUTPUT_DIR: ${{ github.workspace }}/newsletters
    
    - name: Vérifier les fichiers générés
      run: |
        echo "Contenu du dossier newsletters:"
        ls -la newsletters/ || echo "Le dossier newsletters n'existe pas ou est vide"
        
        if [ -f "newsletters/latest.html" ]; then
          echo "Le fichier latest.html existe. Taille:"
          wc -l newsletters/latest.html
        else
          echo "ERREUR: Le fichier latest.html n'existe pas!"
        fi
    
    - name: Préparer le dossier de destination
      run: mkdir -p newsletter-portfolio/newsletters
    
    - name: Copier les fichiers générés vers newsletter-portfolio
      run: |
        # Copier tous les fichiers HTML
        cp -r newsletters/*.html newsletter-portfolio/newsletters/ || echo "Aucun fichier HTML à copier"
        
        # Copier le dossier img s'il existe
        if [ -d "newsletters/img" ]; then
          mkdir -p newsletter-portfolio/newsletters/img
          cp -r newsletters/img/* newsletter-portfolio/newsletters/img/ || echo "Aucune image à copier"
        fi
        
        # Créer fichier index.html simple
        echo "<html><head><title>Newsletter Portfolio</title><meta http-equiv='refresh' content='0;url=./newsletters/latest.html'></head><body>Redirection...</body></html>" > newsletter-portfolio/index.html
        
        # Créer .nojekyll
        touch newsletter-portfolio/.nojekyll
    
    - name: Vérifier les fichiers avant déploiement
      run: |
        echo "Contenu du dossier newsletters :"
        ls -la newsletters/ || echo "Le dossier newsletters n'existe pas"
        
        echo "Contenu du dossier newsletter-portfolio/newsletters :"
        ls -la newsletter-portfolio/newsletters/ || echo "Le dossier newsletter-portfolio/newsletters n'existe pas"
    
    - name: Déploiement sur GitHub Pages
      uses: JamesIves/github-pages-deploy-action@v4
      with:
        repository: SiaSia-dev/newsletter-portfolio
        branch: gh-pages
        folder: newsletter-portfolio
        token: ${{ secrets.DEPLOY_TOKEN }}
        clean: true
        
    - name: Envoyer une notification par email
      uses: dawidd6/action-send-mail@v3
      with:
        server_address: ${{ secrets.MAIL_SERVER }}
        server_port: ${{ secrets.MAIL_PORT }}
        username: ${{ secrets.MAIL_USERNAME }}
        password: ${{ secrets.MAIL_PASSWORD }}
        subject: 🔔 Newsletter Portfolio générée - À vérifier
        body: |
          Bonjour,
          
          La newsletter de cette semaine a été générée et est prête pour votre validation.
          
          URL de prévisualisation: https://siasia-dev.github.io/newsletter-portfolio/newsletters/latest.html
          
          Si le contenu vous convient, vous pouvez déclencher manuellement la publication LinkedIn 
          via l'interface GitHub Actions.
          
          Bonne journée !
        to: ${{ secrets.NOTIFICATION_EMAIL }}
        from: Newsletter Portfolio <${{ secrets.MAIL_USERNAME }}>